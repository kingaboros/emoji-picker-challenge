import React from 'react';
import * as classes from './ChatInput.module.scss';

const ChatInput = () => {
  return <div className={classes.inputWrapper}>Hello from chat input</div>;
};

export default ChatInput;
